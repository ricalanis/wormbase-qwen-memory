# --- Planner provider switch (same code; pick where Qwen runs) ---------------
# auto (default): Qwen-Plus if DASHSCOPE_API_KEY set, else deterministic rules.
# dashscope:    Qwen-Plus on Alibaba Cloud (the submission's required path).
# ollama_cloud: a large Qwen on Ollama Cloud (qwen3-coder:480b) — real model,
#               runs through the local toolchain, great for a $0 local demo.
# local:        small local Ollama model (experimental; weak at plan JSON).
# rules:        deterministic, no model.
WBM_PROVIDER=auto
OLLAMA_CLOUD_MODEL=qwen3-coder:480b   # or qwen3.5:397b  (used when WBM_PROVIDER=ollama_cloud)
# Ollama Cloud key: OLLAMA_API_KEY env, else auto-read from ~/.local/share/opencode/auth.json

# --- Qwen Cloud (Alibaba Cloud Model Studio / DashScope) ---------------------
# Get a key: https://bailian.console.alibabacloud.com  (claim hackathon credits)
DASHSCOPE_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# International (Singapore) compatible-mode endpoint. For Mainland use:
#   https://dashscope.aliyuncs.com/compatible-mode/v1
DASHSCOPE_BASE_URL=https://dashscope-intl.aliyuncs.com/compatible-mode/v1
QWEN_PLANNER_MODEL=qwen-plus

# --- Local small-Qwen triage worker (cameo, via Ollama) — free -------------
WBM_USE_LOCAL_QWEN=0
OLLAMA_BASE_URL=http://localhost:11434/v1
OLLAMA_MODEL=qwen3:1.7b   # any pulled model works; set WBM_USE_LOCAL_QWEN=1 to enable

# --- Storage -----------------------------------------------------------------
WBM_LEDGER_DB=./wbm_ledger.db
